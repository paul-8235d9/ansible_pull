- hosts: localhost
  connection: local
  become: true

  tasks:
  - name: Install packages
    apt:
      name:
        - gparted
        - htop
        - snowflake
        - sublime-text
        - thunderbird
        - visual-studio-code
